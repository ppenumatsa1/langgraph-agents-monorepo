run:
	uvicorn app.main:app --reload

test:
	pytest

lint:
	@echo "lint: configure ruff/black when ready"

evals:
	@echo "evals: add evaluation runner"

build:
	@echo "build: docker build -t researcher-agent ."
